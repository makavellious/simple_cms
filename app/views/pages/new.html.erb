<%= link_to("<< Back to List", pages_path, :class => 'back-link') %>

<div class="pages new">
  <h2>Create Page</h2>


	<%= form_for(@page) do |f| %>

	<%= render(:partial => 'form', :locals => {:f => f}) %>

	  <tr><td><%= f.submit("Create Page") %></td></tr>
	</table>
	<% end %>

</div>