<% @page_title = "Edit Subject '#{@subject.name}'" %>
<%= link_to("<< Back to List", subjects_path, :class => 'back-link') %>

<div class="subjects edit">
  <h2>Edit Subject</h2>


	<%= form_for(@subject) do |f| %>
	<%= render(:partial => 'form', :locals => {:f => f}) %>

	  <tr><td><%= f.submit("Update Subject") %></td></tr>
	</table>
	<% end %>
  
</div>